import "./App.css";

function App() {
    alert(Object.keys(window));
    // alert(JSON.stringify(window));
    return (
        <div className="App">
            <header className="App-header"></header>
        </div>
    );
}

export default App;
